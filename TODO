1. finish function to init the t_cmd struct,
2. write function to execute the processes (they need to pipe infinitely using the fd's provided in t_cmd,
3. add exit, unset, finish export,
4. test builtins,
5. write the function handling the redirect functionality,
6. make sure to handle signals when different processes are running,
7. make the file structure make sense,
8. clean up the .h file,
9. Norm fixes,
