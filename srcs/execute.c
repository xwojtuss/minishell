#include "minishell.h"

int	execute(t_shell *shell)
{
	//execute the commands in shell->cmd
	//each command will be its own process
	//the output of the previous command will be the input of the next command
	
}
